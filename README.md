# Installation and running the app 🚢

## Clone repo 📁

```bash
git clone https://github.com/jedromz/wrshps.git
  ```
  ## Build the app 🏗️
  ```bash
  cd wrshps
  ```
  ```bash
  go build -o wrshps ./cmd 
  ```
  ## Run the binary 🏃
   ```bash
  ./wrshps
  ```

    
